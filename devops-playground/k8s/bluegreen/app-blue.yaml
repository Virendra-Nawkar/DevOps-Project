# =============================================================================
# App Blue  –  v1.0.0  (stable version)
# Namespace: devops
# Internal only – accessed via router service
# =============================================================================

apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-blue
  namespace: devops
  labels:
    app: app-blue
    version: "1.0.0"
    track: blue
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-blue
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: app-blue
        version: "1.0.0"
        track: blue
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      containers:
      - name: app
        image: devops-playground:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 5000
        env:
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: POD_IP
          valueFrom:
            fieldRef:
              fieldPath: status.podIP
        - name: NODE_NAME
          valueFrom:
            fieldRef:
              fieldPath: spec.nodeName
        - name: NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: APP_VERSION
          value: "1.0.0"
        - name: APP_COLOR
          value: "blue"
        resources:
          requests:
            cpu: "200m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 15
          periodSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: 5000
          initialDelaySeconds: 10
          periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: app-blue
  namespace: devops
  labels:
    app: app-blue
    track: blue
spec:
  selector:
    app: app-blue
  type: ClusterIP
  ports:
  - name: http
    port: 5000
    targetPort: 5000
